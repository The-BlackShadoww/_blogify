import CreateForm from "../../components/Blog/CreateForm";

const WriteBlog = () => {
    return (
        <main>
            <section>
                <div className="container">
                    <CreateForm />
                </div>
            </section>
        </main>
    );
};

export default WriteBlog;
